# Контекст дипломної роботи

Цей файл ведеться автоматично під час розробки і містить матеріал для написання дипломної роботи.
Після кожної значущої зміни в проєкті — сюди додається опис рішення, обґрунтування вибору технологій тощо.

---

## Тема та мета

**Тема:** Веб-застосунок для управління торговою точкою

**Мета:** Розробити повноцінний веб-застосунок що дозволяє власнику або менеджеру торгової точки вести облік продажів, контролювати інвентар складу та переглядати аналітику у зручному інтерфейсі.

**Практична цінність:** Замінює ручний облік в Excel або зошиті на структурований цифровий інструмент з реальною базою даних, авторизацією та візуалізацією даних.

---

## Стек технологій

### Frontend
- **Next.js 16 (App Router)** — React-фреймворк з серверним рендерингом. Обраний через вбудований роутинг, підтримку TypeScript та оптимізацію продуктивності.
- **React 19** — бібліотека для побудови UI на основі компонентів.
- **TypeScript** — статична типізація що зменшує кількість помилок під час розробки.
- **Tailwind CSS v3** — utility-first CSS фреймворк для швидкої стилізації без написання окремих CSS файлів.
- **shadcn/ui** — набір готових доступних UI-компонентів побудованих на Radix UI. Не є npm-пакетом — компоненти копіюються в проєкт і повністю кастомізуються.
- **Recharts** — бібліотека для побудови графіків на основі React та SVG.

### Backend / Інфраструктура
- **Supabase** — Backend-as-a-Service на базі PostgreSQL. Надає: базу даних, REST і Realtime API, авторизацію користувачів, безкоштовний хостинг. Обраний як альтернатива власному серверу що спрощує розробку і дозволяє зосередитись на бізнес-логіці.

### Інструменти розробки
- **ESLint** — статичний аналіз коду для виявлення помилок і дотримання стилю.
- **date-fns v3** — бібліотека для роботи з датами. Обрана через модульність (імпортуються тільки потрібні функції) та підтримку локалізації (українська мова для відображення дат).

---

## Архітектура застосунку

### Маршрутизація
Застосунок використовує Next.js App Router з route group `(dashboard)` що об'єднує всі захищені сторінки:

```
/login        — вхід в систему
/dashboard    — головна сторінка з графіками та KPI
/sales        — облік продажів (CRUD)
/inventory    — управління інвентарем (CRUD)
/reports      — звіти з фільтрацією по датах
```

Неавторизований користувач автоматично перенаправляється на `/login`.

### Управління станом
Глобальний стан реалізований через React Context (`StoreProvider`). Провайдер підключений на рівні layout захищеної зони і надає всім дочірнім компонентам доступ до даних та CRUD-операцій без зовнішніх бібліотек (Redux, Zustand тощо).

### Шари даних
1. **`lib/store.ts`** — типи даних, функції агрегації (підрахунок виручки, середнього чеку тощо)
2. **`lib/db.ts`** — всі запити до Supabase (fetchSales, insertSale, updateSale, deleteSale, fetchInventory, insertInventoryItem, updateInventoryItem, deleteInventoryItem). Відповідає за маппінг camelCase ↔ snake_case між TypeScript і PostgreSQL.
3. **`lib/store-context.tsx`** — React Context з CRUD-операціями, станами завантаження та помилок. Викликає функції з `lib/db.ts`, оновлює локальний стан оптимістично.
4. **Supabase** — персистентне сховище (PostgreSQL), REST API, Auth

---

## Функціональні вимоги

- [x] Авторизація користувача (логін / логаут)
- [x] Перегляд дашборду з KPI за обраний період
- [x] Графік продажів по днях
- [x] Додавання, редагування, видалення записів продажів
- [x] Управління інвентарем: додавання товарів, коригування кількості
- [x] Сповіщення про товари з кількістю нижче мінімуму
- [x] Фільтрація і перегляд звітів по датах
- [x] Експорт звітів у CSV
- [x] Сторінка профілю з завантаженням аватара
- [x] Зміна пароля
- [x] Налаштування магазину (назва, адреса, валюта, часовий пояс)
- [x] Реальний курс валют з Monobank API

---

## Журнал рішень

### 2026-04-28 — Початкова версія
Проєкт згенеровано через v0 (AI-генератор інтерфейсів від Vercel). Базова структура включає всі сторінки та UI-компоненти. Дані на цьому етапі мокові — генеруються в пам'яті при завантаженні.

**Вирішені проблеми при налаштуванні:**
- Конфлікт `date-fns@4` з `react-day-picker@8` — понижено до `date-fns@3`, оновлено `react-day-picker` до v9
- `react-day-picker` v9 має інший API `classNames` — оновлено `calendar.tsx` під новий формат
- Видалено зайву залежність `@tailwindcss/postcss` (для Tailwind v4) що конфліктувала з конфігом v3

### 2026-04-30 — Підключення Supabase: реальна БД, авторизація, обробка помилок

**Що зроблено:**

Замінено мокові дані на реальну базу даних Supabase (PostgreSQL). Застосунок тепер повністю персистентний — всі дані зберігаються між перезавантаженнями.

1. **База даних:** Створено дві таблиці у Supabase — `sales` і `inventory_items` з відповідними колонками. Увімкнено Row Level Security (RLS) з політиками що дозволяють доступ тільки авторизованим користувачам.

2. **Шар запитів (`lib/db.ts`):** Всі операції з БД ізольовані в окремому модулі. Маппінг між camelCase (TypeScript) і snake_case (PostgreSQL) виконується тут — компоненти ніколи не бачать формат БД напряму.

3. **Реальна авторизація:** Замінено fake-логін на `supabase.auth.signInWithPassword()`. Після успішного входу — перенаправлення через `window.location.href` (а не `router.push`) щоб примусово перезавантажити сторінку і застосувати cookies.

4. **Захист маршрутів (`proxy.ts`):** Next.js 16 використовує `proxy.ts` замість `middleware.ts` (і функція називається `proxy`, не `middleware`). Файл перевіряє auth-cookie через `createServerClient` з `@supabase/ssr` і перенаправляє неавторизованих на `/login`.

5. **Cookie-сесія:** Критичне рішення — використано `createBrowserClient` з `@supabase/ssr` замість `createClient` з `@supabase/supabase-js`. Причина: стандартний клієнт зберігає сесію в `localStorage`, але `proxy.ts` читає тільки cookies. `createBrowserClient` автоматично синхронізує сесію в cookies — без цього авторизація "успішно" проходила, але proxy не бачив токен і продовжував редиректити на `/login`.

6. **Стани завантаження та помилок:** Кожна сторінка тепер має три стани: завантаження (спіннер), помилка (іконка + повідомлення + кнопка "Спробувати знову"), успіх (нормальний контент). Логіка розміщена в `store-context.tsx`, сторінки тільки читають `salesLoading`, `salesError` тощо.

7. **Спіннер:** Реалізований через CSS `conic-gradient` з `mask` анімацією — без зовнішніх бібліотек. Колір береться з CSS-змінної `hsl(var(--primary))` що автоматично підлаштовується під тему.

8. **Логаут:** Кнопка виходу в top-bar dropdown викликає `supabase.auth.signOut()` і робить `window.location.href = "/login"` для повного перезавантаження і очистки cookies.

### 2026-05-01 — Рефакторинг: розбиття сторінок на компоненти

Всі головні сторінки (`sales`, `inventory`, `dashboard`, `reports`, `profile`, `settings`, `login`) розбиті на менші компоненти за патерном Feature-Sliced Design. Кожна сторінка має папку `components/` з підпапками для кожного логічного блоку.

**Мотивація:** сторінки розрослись до 200–300 рядків — складно читати і підтримувати. Після рефакторингу `page.tsx` містить тільки стан і хендлери, UI розподілений по компонентах.

**Патерн:** `app/(dashboard)/<page>/components/<componentName>/index.tsx` — кожен компонент в окремій папці для можливості додавання локальних стилів, типів і підкомпонентів.

### 2026-05-01 — Реорганізація в shared/

Створено папку `shared/` з підпапками:
- `shared/components/` — всі UI-компоненти (перенесено з `components/`)
- `shared/lib/` — store, store-context, supabase клієнт (перенесено з `lib/`)
- `shared/types/` — TypeScript інтерфейси (`SaleEntry`, `InventoryItem`, `StoreProfile`, `UserProfile`)
- `shared/constants/` — константи що перевикористовуються (`CURRENCIES`, `TIMEZONES`, дефолтні профілі)
- `shared/utils/` — чисті утилітарні функції (`getDailyAggregates`, `getTotals`)
- `shared/hooks/` — кастомні React хуки

**Мотивація:** розділення відповідальності — типи, константи і утиліти окремо від компонентів і бізнес-логіки. Спрощує майбутню міграцію на Supabase: мокові дані в `store.ts` можна замінити на реальні запити без торкання решти коду.

**Зворотна сумісність:** `tsconfig.json` оновлено з аліасами `@/components/*` і `@/lib/*` що вказують на нові шляхи — старі імпорти продовжують працювати.

### 2026-05-06 — Кастомні хуки для сторінок

Логіка кожної сторінки винесена в окремий хук:
- `useSalesPage` — фільтри, пагінація, CRUD, модалки
- `useInventoryPage` — пошук, пагінація, CRUD, коригування кількості
- `useDashboardPage` — діапазон дат, агрегація, модалки додавання
- `useReportsPage` — фільтри дат, агрегація, CSV експорт

**Мотивація:** усунення prop drilling — `page.tsx` перетворився на чистий JSX (~20–30 рядків) без логіки. Хуки легко тестувати окремо від UI.

### 2026-05-06 — Профіль користувача та аватар

Додано сторінку `/profile` з можливістю:
- Редагування імені користувача (зберігається в `user_metadata` Supabase)
- Завантаження аватара в Supabase Storage (бакет `avatars`, файл `{userId}.{ext}`)
- Видалення аватара з fallback на ініціали імені

**Технічне рішення аватара:** Supabase Storage повертає публічний URL. До URL додається `?t={timestamp}` щоб обійти кеш браузера при оновленні фото. При видаленні передається `undefined` замість порожнього рядка в `src` — Radix UI `Avatar` тоді автоматично показує `AvatarFallback`.

### 2026-05-06 — Реальний курс валют з Monobank API

Замість захардкоджених курсів (`USD=41.5`, `EUR=45.2`) реалізовано хук `useExchangeRates` що завантажує актуальні курси з публічного API Монобанку (`api.monobank.ua/bank/currency`).

**Деталі:** API повертає курси в форматі ISO 4217 (USD=840, EUR=978, UAH=980). Хук маппить коди на валюти з `CURRENCIES`, використовує `rateSell` як курс продажу. При помилці мережі — залишаються захардкоджені значення як fallback. Курс відображається в налаштуваннях магазину під селектом валюти.
